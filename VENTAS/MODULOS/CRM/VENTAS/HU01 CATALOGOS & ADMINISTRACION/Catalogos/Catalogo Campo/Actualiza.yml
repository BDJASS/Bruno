info:
  name: Actualiza
  type: http
  seq: 3

http:
  method: PUT
  url: http://192.0.1.14:8853/RestADOSAVentas/rest/RestADOSAVentas/Catalogo/Campo/Actualiza?Id=5&Nombre=Numero de guia&Activo=true&IdUser=olds
  params:
    - name: Id
      value: "5"
      type: query
    - name: Nombre
      value: Numero de guia
      type: query
    - name: Activo
      value: "true"
      type: query
    - name: IdUser
      value: olds
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |
  # Servicio REST — Actualización de Campo del Ticket

  ---

  ## 1. Título del Servicio

  **Actualización del Catálogo de Campos del ticket (HU-01, 4.0)**
  Edita el nombre y/o el estado (Activo) de un campo. `Activo` es opcional: si no se envía, se conserva.
  Programa: `catcampo.p` · Procedimiento: `actualizaCampo`.

  ---

  ## 2. Método HTTP

  ```
  PUT
  ```

  ---

  ## 3. URL Completa

  ```
  http://192.0.1.14:8853/RestADOSAVentas/rest/RestADOSAVentas/Catalogo/Campo/Actualiza
  ```

  ---

  ## 4. Parámetros de Entrada (Query String)

  > ✅ = Requerido · ❌ = Opcional

  - **`Id`** · INTEGER · ✅ — Id del campo.
  - **`Nombre`** · STRING · ✅ — Nuevo nombre. Obligatorio, sin duplicar.
  - **`Activo`** · STRING · ❌ — Estado `"true"`/`"false"`. **Si NO se envía (o vacío), se conserva el valor actual** (no se pone en null).
  - **`IdUser`** · STRING · ✅ — Usuario que edita.

  ---

  ## 5. Body

  No aplica — los parámetros viajan en el **Query String** (ver secciones 4 y 7).

  ---

  ## 6. Headers

  | Header         | Valor                                  |
  |----------------|----------------------------------------|
  | `Accept`       | `application/json`                     |
  | `Content-Type` | `application/json`                     |

  ---

  ## 7. Ejemplo de Request

  ```
  PUT http://192.0.1.14:8853/RestADOSAVentas/rest/RestADOSAVentas/Catalogo/Campo/Actualiza?Id=5&Nombre=Numero de guia&Activo=true&IdUser=olds
  ```

  ---

  ## 8. Ejemplo de Response

  ```json
  {
    "IdError": false,
    "Respuesta": "Registro actualizado: 14 - Material Recibido."
  }
  ```

  ---

  ## 9. Campos de la Respuesta

  - **`IdError`** · BOOLEAN — `true` si falló una validación o hubo error; `false` si la operación fue correcta.
  - **`Respuesta`** · CHARACTER — Mensaje amigable de resultado (no expone verbos HTTP ni errores técnicos).

  ---

  ## 10. Reglas y Validaciones

  - **`IdUser` es obligatorio** en todos los endpoints; si viene vacío o nulo → `IdError = true` con `"Se requiere el usuario (IdUser)."`.
  - `Id` debe existir.
  - `Nombre` sin duplicar.
  - `EXCLUSIVE-LOCK NO-WAIT`.
  - Toda Alta / Actualización / Baja registra auditoría (`programas/auditoria.p`, app `oeVentas`).
  - Ante cualquier fallo la respuesta es amigable y `IdError = true`; nunca se realiza la operación a medias (transacción con `UNDO`).

  ---
  **Desarrollado por:** SIS10 - JASS
