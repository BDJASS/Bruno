info:
  name: Actualiza
  type: http
  seq: 3

http:
  method: PUT
  url: http://192.0.1.14:8853/RestADOSAVentas/rest/RestADOSAVentas/Catalogo/Problema/Actualiza?Id=11&Nombre=Material danado&Activo=true&IdUser=olds
  params:
    - name: Id
      value: "11"
      type: query
    - name: Nombre
      value: Material danado
      type: query
    - name: Activo
      value: "true"
      type: query
    - name: IdUser
      value: olds
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: true

docs: |
  # Servicio REST — Actualización de Categoría de Problema

  ---

  ## 1. Título del Servicio

  **Actualización de Categoría de Problema (HU-01, 1.0)**
  Edita el nombre y/o el estado de una categoría (los vínculos se manejan con Asocia/Quita).
  Programa: `catproblema.p` · Procedimiento: `actualizaProblema`.

  ---

  ## 2. Método HTTP

  ```
  PUT
  ```

  ---

  ## 3. URL Completa

  ```
  http://192.0.1.14:8853/RestADOSAVentas/rest/RestADOSAVentas/Catalogo/Problema/Actualiza
  ```

  ---

  ## 4. Parámetros de Entrada (Query String)

  > ✅ = Requerido · ❌ = Opcional

  - **`Id`** · INTEGER · ✅ — Id de la categoría.
  - **`Nombre`** · STRING · ✅ — Nuevo nombre. Obligatorio, sin duplicar.
  - **`Activo`** · STRING · ✅ — Estado `"true"`/`"false"`.
  - **`IdUser`** · STRING · ✅ — Usuario que edita.

  ---

  ## 5. Body

  No aplica — los parámetros viajan en el **Query String** (ver secciones 4 y 7).

  ---

  ## 6. Headers

  | Header         | Valor                                  |
  |----------------|----------------------------------------|
  | `Accept`       | `application/json`                     |
  | `Content-Type` | `application/json`                     |

  ---

  ## 7. Ejemplo de Request

  ```
  PUT http://192.0.1.14:8853/RestADOSAVentas/rest/RestADOSAVentas/Catalogo/Problema/Actualiza?Id=11&Nombre=Material danado&Activo=true&IdUser=olds
  ```

  ---

  ## 8. Ejemplo de Response

  ```json
  {
    "IdError": false,
    "Respuesta": "Registro guardado correctamente."
  }
  ```

  ---

  ## 9. Campos de la Respuesta

  - **`IdError`** · BOOLEAN — `true` si falló una validación o hubo error; `false` si la operación fue correcta.
  - **`Respuesta`** · CHARACTER — Mensaje amigable de resultado (no expone verbos HTTP ni errores técnicos).

  ---

  ## 10. Reglas y Validaciones

  - **`IdUser` es obligatorio** en todos los endpoints; si viene vacío o nulo → `IdError = true` con `"Se requiere el usuario (IdUser)."`.
  - `Id` debe existir.
  - `Nombre` sin duplicar.
  - `EXCLUSIVE-LOCK NO-WAIT`.
  - Toda Alta / Actualización / Baja registra auditoría (`programas/auditoria.p`, app `oeVentas`).
  - Ante cualquier fallo la respuesta es amigable y `IdError = true`; nunca se realiza la operación a medias (transacción con `UNDO`).

  ---
  **Desarrollado por:** SIS10 - JASS
